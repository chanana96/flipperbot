import * as ping from "./ping";
import * as reload from "./reload";
import * as suggest from "./suggest/suggest";
import * as track from "./track/track";

export const commands = {
    ping,
    reload,
    track,
    suggest,
};
